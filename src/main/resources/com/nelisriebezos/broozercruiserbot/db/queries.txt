get_schema_version = 
    select version 
    from   cruiser_version
    where  name = 'cruiser'
;

;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;

all_cars =
    select id, kmcounter
    from car
;

find_car =
    select id, kmcounter
    from car
;

create_car =
    insert into car(id, kmcounter) values (:id, :kmcounter)
;